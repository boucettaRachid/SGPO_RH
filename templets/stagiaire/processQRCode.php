<?php
/*
if ($_SERVER["REQUEST_METHOD"] === "POST") {
    $data = json_decode(file_get_contents("php://input"));

    $date = $data->date;
    $stagiaireId = $data->stagiaireId;
    $scanLimit = $data->scanLimit;

    //require_once("StagiaireController.php");
    require_once("D:\wamp64\www\SGPO_RH\controle\Stagiaire\StagiaireController.php");

    $model = new StagiaireModel(); // Instantiate your model here
    $controller = new StagiaireController($model);

    $controller->processQRCode($date, $stagiaireId, $scanLimit);
}*/
?>